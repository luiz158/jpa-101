spring:
  jpa:
    show-sql: false
    hibernate:
      naming:
        strategy: org.hibernate.cfg.DefaultNamingStrategy
  datasource:
   username: postgres
   password: postgres
   url: jdbc:postgresql://localhost:5432/onetomanyfk